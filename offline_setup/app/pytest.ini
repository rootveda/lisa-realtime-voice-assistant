[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
markers =
    face: Face registry + session API regression
    rag: Local RAG / lexical helpers
    instructions: Instruction preset payload contracts
    regression: Fast deterministic checks (default CI set)
